# Claude Prompt: Generate Spec

Create an implementation-ready spec from the approved PRD.

- Use `agents/shared/templates/spec-template.md`.
- List exact files to create/modify.
- Describe per-file change plan.
- Include concrete validation commands.
- Keep scope tight and avoid unnecessary context.
