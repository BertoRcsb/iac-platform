# local_foundation

Stack that composes local-only Terraform building blocks.

Used by `environments/lab` for a local runnable workflow (`init`, `validate`, `plan`, `apply`) without cloud credentials.
